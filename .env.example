# Salin ke .env dan isi nilai asli Anda sendiri. Jangan pernah commit .env (sudah di .gitignore).
# Nilai di bawah ini contoh yang jelas palsu — bukan kredensial nyata.

# Alamat listen server statis Go. Kosongkan untuk default :8787.
CAPROMPT_ADDR=:8787
